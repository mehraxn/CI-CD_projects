apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kubeops.fullname" . }}-config
  labels:
    {{- include "kubeops.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
